spring:
  application:
    name: miniauthorizer
  data:
    mongodb:
      # Override via MONGODB_URI env var in Docker/production.
      # Default targets a local MongoDB instance for development.
      uri: ${MONGODB_URI:mongodb://user:password@localhost:27017/miniautorizador}

values:
  standardValue: ${VALUES_STANDARD_VALUE:500.00}
